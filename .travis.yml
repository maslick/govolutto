language: go

before_install:
  - go get -t -v ./...

script:
  - go test ./... -coverpkg=./... -coverprofile=coverage.txt -covermode=atomic -race

after_success:
  - bash <(curl -s https://codecov.io/bash)